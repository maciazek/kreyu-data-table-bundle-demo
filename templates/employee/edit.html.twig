{% extends 'base.html.twig' %}

{% block title %}{{ 'app_employee_edit'|trans({}, 'routes') }} • {{ parent() }}{% endblock %}
{% block header %}{{ 'app_employee_edit'|trans({}, 'routes') }}{% endblock %}
{% block toolbar %}
    {{ component('Button', {
        tag: 'a',
        context: 'info',
        href: path('app_employee_show', {id: employee.id}),
        icon: 'arrow-counterclockwise',
        label: 'app_employee_show'|trans({}, 'routes'),
        tooltip: {placement: 'bottom', title: 'app_employee_show'|trans({}, 'routes')},
        responsive: true,
    }) }}
    {{ component('Button', {
        tag: 'button',
        type: 'submit',
        form: form.vars.id,
        context: 'success',
        icon: 'check-lg',
        label: 'save'|trans({}, 'buttons'),
        tooltip: {placement: 'bottom', title: 'save'|trans({}, 'buttons')},
        responsive: true,
    }) }}
    {{ include('employee/_delete_button.html.twig') }}
{% endblock %}

{% block modals %}
    {{ include('employee/_delete_modal.html.twig') }}
{% endblock %}

{% block body %}
    {{ include('employee/_form.html.twig') }}
{% endblock %}
