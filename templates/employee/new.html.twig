{% extends 'base.html.twig' %}

{% block title %}{{ 'app_employee_new'|trans({}, 'routes') }} • {{ parent() }}{% endblock %}
{% block header %}{{ 'app_employee_new'|trans({}, 'routes') }}{% endblock %}
{% block toolbar %}
    {{ component('Button', {
        tag: 'button',
        type: 'submit',
        form: form.vars.id,
        context: 'success',
        icon: 'check-lg',
        label: 'save'|trans({}, 'buttons'),
        tooltip: {placement: 'bottom', title: 'save'|trans({}, 'buttons')},
        responsive: true,
    }) }}
{% endblock %}

{% block body %}
    {{ include('employee/_form.html.twig') }}
{% endblock %}
