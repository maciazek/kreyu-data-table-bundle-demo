{% extends 'base.html.twig' %}

{% block title %}{{ 'app_target_new'|trans({}, 'routes') }} • {{ parent() }}{% endblock %}
{% block header %}{{ 'app_target_new'|trans({}, 'routes') }}{% endblock %}
{% block toolbar %}
    {{ component('Button', {
        tag: 'a',
        context: 'info',
        href: path('app_contract_show', {id: target.contract.id}),
        icon: 'arrow-counterclockwise',
        label: 'app_contract_show'|trans({}, 'routes'),
        tooltip: {placement: 'bottom', title: 'app_contract_show'|trans({}, 'routes')},
        responsive: true,
    }) }}
    {{ component('Button', {
        tag: 'button',
        type: 'submit',
        form: form.vars.id,
        context: 'success',
        icon: 'check-lg',
        label: 'save'|trans({}, 'buttons'),
        tooltip: {placement: 'bottom', title: 'save'|trans({}, 'buttons')},
        responsive: true,
    }) }}
{% endblock %}

{% block body %}
    {{ include('target/_form.html.twig') }}
{% endblock %}
