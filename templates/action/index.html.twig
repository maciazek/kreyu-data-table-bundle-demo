{% extends 'base.html.twig' %}

{% block title %}{{ 'app_action_index'|trans({}, 'routes') }} • {{ parent() }}{% endblock %}
{% block header %}{{ 'app_action_index'|trans({}, 'routes') }}{% endblock %}
{% block toolbar %}
    {{ component('Button', {
        tag: 'button',
        context: 'outline-primary',
        icon: 'braces',
        'data-bs-toggle': 'modal',
        'data-bs-target': '#sourceCodeModal',
        label: 'source_code'|trans({}, 'buttons'),
        tooltip: {placement: 'bottom', title: 'source_code'|trans({}, 'buttons')},
        responsive: true,
    }) }}
{% endblock %}

{% block modals %}
    {{ component('SourceCodeModal', {classes: source_code_classes}) }}
{% endblock %}

{% block body %}
    <div class="card">
        {{ data_table(employees) }}
    </div>
{% endblock %}
